package build.desktop.example.glfw

import mill.*
import mill.javalib.*
import mill.javalib.publish.*
import mill.scalalib.{AssemblyModule, SbtModule}
import mill.util.JarManifest
import com.goyeau.mill.scalafix.ScalafixModule

object `package` extends SbtModule with PublishModule  with AssemblyModule with ScalafixModule:
  override def artifactName = "desktop-example-glfw"

  override def scalaVersion = build.versions.scala

  def scalacOptions = super.scalacOptions() ++ Seq(
    "-Wunused:imports",
    "-Xkind-projector",
    //"-Ximplicit-search-limit", "10",
    "-Vprofile",
  )

  override def mvnDeps = super.mvnDeps() ++ build.dependencies.http4s ++ build.dependencies.logging

  override def forkArgs = Seq(
    "-XstartOnFirstThread"
  )

  override def moduleDeps = super.moduleDeps ++ Seq(build.desktop.example.shared) ++ Seq(build.desktop.windowing.glfw)

  override def pomSettings = build.pomSettings("desktop-example-cats")

  override def publishVersion = build.publishVersion

  override def mainClass = Some("gui4s.desktop.example.glfw.ScenedExample")

  override def manifest : T[JarManifest] = Task:
    super.manifest().add(("Multi-Release" -> "true"))
end `package`